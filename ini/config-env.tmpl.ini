db.host = db-host
db.name = db-name
db.user = db-user
db.password = db-password

mail.host = mail-host
mail.username = mail-username
mail.password = mail-password
mail.port = mail-port
mail.to = mail-to
mail.from = mail-from

runtime.displayErrors = on-off

session.domain = my-domain.com

key.jwtSecret = some-key